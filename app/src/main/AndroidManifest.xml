<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="br.com.victoor.treinamentos">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup">
        <activity android:name=".Screen">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".MenuUserGerenciamento" />
        <activity android:name=".CadastroUser" />
        <activity android:name=".ExercicioCadastro" />
        <activity android:name=".MembroCadastro" />
        <activity android:name=".TreinoGetActivity" />
        <activity android:name=".UserAtivity" />
        <activity android:name=".TreinoCadastro" />
        <activity android:name=".LoginActivity"/>
        <activity android:name=".AdminActivity" />
    </application>

</manifest>